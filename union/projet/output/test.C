void find_occu(char tab[TAB_SIZE], int buf[TAB_SIZE][2], int *taille){
    int nb_occu = *taille;
    for(int i = 0; i < TAB_SIZE; i++){
        int temp = i;
        if(tab[temp]== 'D'){
            if(tab[temp]== 'G'){
                while(tab[temp]== 'G'){
                    temp++;
                    if(tab[temp]== 'H'){

                }
               
        }
    }
}